name: unilab_order_01
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path: ^1.8.0
  provider: ^6.0.0
  intl: ^0.17.0
  uuid: ^4.2.2
  shortid: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0  

flutter:
  uses-material-design: true
  assets:
    - assets/unilab.json
 
